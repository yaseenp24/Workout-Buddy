version: "3.8"
services:
  backend:
    build: .
    container_name: workoutbuddy-backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - workoutbuddy_data:/app/instance
    restart: unless-stopped

volumes:
  workoutbuddy_data:



